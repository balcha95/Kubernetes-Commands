# Kubernetes Commands

List of general purpose commands for Kubernetes management:

### Informational

```
$ kubectl get pods
$ kubectl get pods --all-namespaces
```

- $ kubectl scale
- $ kubectl label
- $ kubectl get 
  - pods
  - deployments
  - rs | replicasets
  - logs
  - run 

